---
title: "Get Your Human-Readable Identity"
seoTitle: "NIP-05 Explained: Get a Verified Nostr Address"
description: "Set up NIP-05 for a professional, easy-to-share Nostr identifier like you@domain.com"
estimatedTime: "10-15 minutes"
priority: 2
category: "intermediate"
prerequisites: ["keys-and-security", "quickstart"]
---

import { NIP05Checker } from "@components/interactive/NIP05Checker";
import { CTA } from "@components/ui/CTA";
import { Note } from "@components/ui/Note";
import { Callout } from "@components/ui/Callout";
import { NIP05IdentityQuiz } from "@components/interactive/NIP05IdentityQuiz";

## What is NIP-05? 🆔

NIP-05 (Nostr Implementation Possibility #5) lets you have a human-readable identifier like `you@domain.com` instead of a long string of random characters.

### The Problem It Solves

<DiagramCompare
  panels={[
    {
      title: "Before NIP-05",
      lines: ['Follow: npub1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', '"Hey, follow me! I\'m npub1abc123…xyz789"'],
    },
    {
      title: "After NIP-05",
      tone: "positive",
      lines: ['Follow: alice@example.com', '"Hey, follow me! I\'m alice@example.com"'],
    },
  ]}
  caption="The same identity, in a form a human can actually repeat out loud."
/>

### What You Get

- ✅ **Easy to share**: "Find me at bob@nostrplebs.com"
- ✅ **Looks professional**: Clean, memorable identifier
- ✅ **Trust signal**: NIP-05 badge in many clients
- ✅ **Easier to find**: Search by your identifier

### But Remember...

> ⚠️ **NIP-05 is optional.** You can use Nostr perfectly well with just your npub.

The NIP-05 identifier is a **convenience**, not a requirement. Your `npub` is your real identity on Nostr.

---

## Why Get a NIP-05? ✨

### Easier to Share

```
At a party:
"What's your Nostr?"

Without NIP-05:
"Uh... n-p-u-b-one-x-x-x... let me find it in my phone"

With NIP-05:
"Just alice@example.com"
```

### Professional Appearance

- **Businesses**: `support@yourcompany.com`
- **Creators**: `artist@yourdomain.com`
- **Developers**: `dev@opensourceproject.com`

### Trust & Authenticity

Many clients show a ✓ badge for NIP-05 users:

<MockProfile
  name="Alice"
  handle="alice@example.com"
  bio="Bitcoin educator"
  verified
  caption="A verified NIP-05 address shown the way most clients render it."
/>

This helps people know they're following the real you, not an impostor.

### Do You Need One?

**No.** You can use Nostr perfectly fine without it.

**But...** it's highly recommended if you're:

- Building a personal brand
- Creating content
- Running a business
- Serious about Nostr long-term
- Tired of reading 63-character strings

### Not Centralization!

Some worry NIP-05 means "centralized identity." It doesn't:

- You can have **multiple** NIP-05 identifiers
- You can **change** them anytime
- Your **npub** is still your real identity
- NIP-05 is just a **pointer** to your npub

---

## Free Options 🎁

Several services offer free NIP-05 identifiers. Great for trying it out!

### NostrCheck

Another great free option.

**How to get one:**

1. Visit [nostrcheck.me](https://nostrcheck.me)
2. Create account
3. Set your NIP-05
4. Verify your npub

**Pros:**

- Also free
- Additional tools included

### Other Free Providers

- **bitcoiner.social**: Bitcoin-focused community
- **getalby.com**: If you use Alby wallet
- **stacker.news**: For SN users
- **NostrCheck.me** - Additional tools included

<Note variant="info">
  Free providers may have waiting lists or verification requirements. Be
  patient!
</Note>

<Callout variant="warning">
  Free providers can change terms or shut down. If you become serious about
  Nostr, consider upgrading to a paid option or self-hosting.
</Callout>

---

## Paid Options

### NostrPlebs

One of the most popular paid NIP-05 providers. Pricing is based on username length and paid in Bitcoin satoshis (sats).

**Cost:**

| Username Length | Price (sats/year) |
|-----------------|-------------------|
| 2 characters   | 65,000 sats (~$40) |
| 3 characters   | 45,000 sats (~$28) |
| 4 characters   | 22,500 sats (~$14) |
| 5+ characters  | 12,500 sats (~$8)  |

*Note: Some special names may have increased prices. Prices in USD are approximate based on current Bitcoin rates.*

**Domains Available:**

- `nostrplebs.com` - Basic membership
- `nostriches.net` - Basic membership  
- `nostr.fan` - Basic membership
- `nostrich.zone` - Basic membership
- `nostrpurple.com` - Premium membership

**What's Included:**

- Human-readable NIP-05 identifier
- Lightning Address support (receive sats to your identifier)
- Email forwarding address
- Access to Nostr Plebs relay
- Verified checkmark badge in clients
- Directory listing to help others find you

**Setup:**

1. Visit [nostrplebs.com](https://nostrplebs.com)
2. Choose your preferred domain
3. Enter your desired username
4. Pay the Lightning invoice
5. Add your npub via the management portal
6. Done!

### Other Paid Providers

- **NostrName.com** - Various pricing tiers
- **NostrID.com** - Domain + NIP-05 packages
- **Custom domain registrars** - Many now offer NIP-05 setup

**What you get:**

- Professional appearance
- Better uptime guarantees
- Support if issues arise
- Often bundled with other services

| Method | Cost | Difficulty | Control | Best For |
|--------|------|------------|---------|----------|
| Free Provider | $0 | Easy | Low | Beginners |
| Paid Provider | ~$15/yr | Easy | Medium | Serious Users |
| Self-hosted | Domain cost | Advanced | High | Tech-savvy |

---

## Using Your Own Domain 🌐

For complete control and professionalism, use your own domain.

### Step 1: Buy a Domain

Any domain registrar works:

- [Namecheap](https://namecheap.com)
- [Porkbun](https://porkbun.com)
- [Cloudflare Registrar](https://www.cloudflare.com/products/registrar/)

Cost: Usually $10-15/year

### Step 2: Create the nostr.json File

Host a file at `https://yourdomain.com/.well-known/nostr.json`:

```json
{
  "names": {
    "alice": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9"
  }
}
```

Your identifier becomes: `alice@yourdomain.com`

<Callout variant="warning" title="Use your hex public key, not your npub">
  NIP-05 requires the raw lowercase-hex form of your public key. If you paste
  `npub1...` into `nostr.json`, clients will fail to verify you — and they fail
  silently, with no message explaining why.

Most clients show the hex form under Settings → Keys, labelled "public key
(hex)". You can also paste your npub into the checker further down this page,
which displays both forms side by side.

</Callout>

**No DNS records are involved.** NIP-05 verification is an ordinary HTTPS
request to `/.well-known/nostr.json`. You do not need a TXT record, a CNAME, or
any DNS entry beyond whatever already points your domain at your web host.

### Example Setup Walkthrough

**Scenario:** You own `alice.dev`

1. **Create the JSON file:**

```json
{
  "names": {
    "alice": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9"
  }
}
```

2. **Upload to your server:**

```
https://alice.dev/.well-known/nostr.json
```

3. **Verify it works:**
   Open in browser: `https://alice.dev/.well-known/nostr.json`

4. **Add to your Nostr profile:**
   In your client: Settings → Profile → NIP-05 → `alice@alice.dev`

---

## Testing Your NIP-05 ✅

### Check Your Identifier

Use our tool below or visit your identifier URL:

<NIP05Checker showForm={true} client:idle />

### In Your Client

Most clients show NIP-05 status:

**Damus/Amethyst:**

- Settings → Profile → NIP-05 field
- Shows ✓ when set up correctly

**Primal:**

- Profile shows NIP-05 badge
- Click badge to see details

### Troubleshooting Common Issues

**"NIP-05 not working"**

- **Check the key format first.** `nostr.json` must contain your public key in
  lowercase hex — 64 characters, no `npub1` prefix. An `npub` here is the single
  most common cause of a NIP-05 that never verifies.
- Ensure the JSON is valid (use a JSON validator)
- Check that the file is accessible at the exact URL

**"File not found"**

- Make sure `.well-known` folder is created
- Check your server serves hidden directories
- Verify the path: `/.well-known/nostr.json`

**"Domain not resolving"**

- DNS changes can take up to 24 hours
- Clear your DNS cache
- Check with `dig` or `nslookup` command

**"Client doesn't recognize it"**

- Some clients cache NIP-05 lookup results
- Try logging out and back in
- Wait a few minutes for propagation

**"Verification Failed" Error**

**Possible causes:**

1. **JSON syntax error** - Check for missing quotes or commas
2. **Wrong file location** - Must be at `/.well-known/nostr.json`
3. **Caching** - Wait 10-15 minutes and retry
4. **HTTPS required** - Must be served over HTTPS

**Fix:**

- Validate JSON at jsonlint.com
- Check file is at exact path
- Clear client cache
- Ensure SSL certificate is valid

**"Not Found" Error**

**Possible causes:**

1. File not uploaded to correct location
2. Server blocks dot-directories
3. Wrong domain

**Fix:**

- Verify file exists at exact URL
- Check `.htaccess` or server config allows dot-directories
- Confirm you're using correct domain

**Provider-Specific Issues**

**NostrPlebs:**

- Check spam folder for verification email
- Ensure npub is correct (not nsec!)
- Contact support if stuck

**Self-hosted:**

- Verify DNS propagation: `dig yourdomain.com`
- Check server logs for errors
- Ensure file permissions allow reading

<Callout variant="warning">
  Never share your nsec (private key) when setting up NIP-05. Only your npub
  (public key) is needed.
</Callout>

---

## Advanced Tips 💡

### Multiple Identities

You can have multiple NIP-05 addresses:

```
personal@yourdomain.com
work@yourdomain.com
blog@yourdomain.com
```

All point to the same npub, or different ones.

### Subdomains

Use subdomains for organization:

```
alice@team.yourdomain.com
alice@community.yourdomain.com
```

### Hosting Multiple Users

You can host multiple NIP-05 identifiers on one domain:

```json
{
  "names": {
    "alice": "b0635d6a9851d3aed0cd6c495b282167acf761729078d975fc341b22650b07b9",
    "bob": "3bf0c63fcb93463407af97a5e5ee64fa883d107ef9e558472c4eb9aaaefa459d",
    "carol": "82341f882b6eabcd2ba7f1ef90aad961cf074af15b9ef44a09f9d2a8fbfbe6a2"
  }
}
```

### Verifying Your Setup

**Method 1: Your Client**

1. Add the NIP-05 to your profile
2. Save changes
3. Look for a checkmark ✓ or "verified" badge
4. May take a few minutes to propagate

**Method 2: Nostr Explorer**

1. Visit a Nostr explorer like nostr.guru
2. Search for your NIP-05
3. Check if it resolves to your npub

**Method 3: Direct Test (Command Line)**

```bash
curl https://yourdomain.com/.well-known/nostr.json
```

**What Success Looks Like:**

- ✅ Client shows verification badge
- ✅ Others can find you by your NIP-05
- ✅ Your profile displays your human-readable name

### Privacy Considerations

- NIP-05 links your npub to a domain
- This could deanonymize you if the domain has your real info
- Use free providers for pseudonymous accounts
- Use your own domain when you want to be identified

---

## Test Your NIP-05 Knowledge

Think you understand NIP-05? Take the quiz:

<NIP05IdentityQuiz client:idle />

---

## Resources

- [NIP-05 Specification](https://github.com/nostr-protocol/nips/blob/master/05.md)
- [NostrPlebs](https://nostrplebs.com) - Paid NIP-05
- [NostrCheck](https://nostrcheck.me) - Free NIP-05 + tools
- [NIP05 Generator](https://github.com/nostr-tools/nip05) - DIY tools

---

## Keep reading

- [Check any NIP-05 address](/tools/nip05-checker) — verify a `name@domain` resolves to the key it claims
- [Quickstart: launch in 5 minutes](/guides/quickstart) — if you do not have an account yet, start here
