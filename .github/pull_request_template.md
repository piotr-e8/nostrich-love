## What does this change?

## Why?

## Checklist

- [ ] `npm run typecheck` — 0 errors
- [ ] `npm run test` — all passing
- [ ] `npm run build` succeeds
- [ ] Content changes: the English source and affected translations stay in sync
      (or the PR says which translations still need porting)
- [ ] Locale paths go through `src/i18n/paths.ts` (never hand-built URLs)
